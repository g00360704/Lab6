// Define a functional React component named 'Footer'
function Footer(){
    return (
        <div>
            <h2>My Footer in another component</h2> <!-- Display a footer heading -->
        </div>
    );
}

// Export the 'Footer' component so it can be used in other parts of the application
export default Footer;
